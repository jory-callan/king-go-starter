package auth_token

import (
	"github.com/labstack/echo/v4"
)

// RegisterRoutes registers the auth token routes
func RegisterRoutes(g *echo.Group) {
	// This module provides authentication token management functionality
	// but doesn't have specific routes - it provides middleware and utility functions
}